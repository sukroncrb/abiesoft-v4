<?php

declare(strict_types=1);

use Abiesoft\System\Console\Kernel;
use Dotenv\Dotenv;

require_once __DIR__ . '/vendor/autoload.php';

$dotenv = Dotenv::createImmutable(__DIR__);
$dotenv->safeLoad();

$kernel = new Kernel();
$kernel->handle($argv);